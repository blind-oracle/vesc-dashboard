# vesc-dashboard partition table for 4 MB flash (same layout as the Arduino core's default.csv):
# two 1.25 MB app slots so OTA stays possible, a 1.375 MB filesystem slot (unused so far),
# a 64 KB core-dump slot. CONFIG_PARTITION_TABLE_CUSTOM_FILENAME in sdkconfig.defaults and
# board_build.partitions in platformio.ini both point here.
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x140000,
app1,     app,  ota_1,   0x150000,0x140000,
spiffs,   data, spiffs,  0x290000,0x160000,
coredump, data, coredump,0x3F0000,0x10000,
