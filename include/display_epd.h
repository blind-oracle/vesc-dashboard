// Legacy e-paper dashboard (Good Display GDEY042T81, GxEPD2 over SPI).
// Only compiled when DISPLAY_TYPE == DISPLAY_TYPE_EPD_GDEY042T81; the entry
// point display_start() is declared in the generic display.h.
#pragma once
#include "display.h"
